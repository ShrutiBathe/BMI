import javax.swing.*;

public class PurchasePage extends JFrame {
    public PurchasePage() {
        setTitle("Purchase Management");
        setSize(400, 300);
        setDefaultCloseOperation(DISPOSE_ON_CLOSE);
        initComponents();
    }

    private void initComponents() {
        // Purchase form fields and buttons
    }
}
